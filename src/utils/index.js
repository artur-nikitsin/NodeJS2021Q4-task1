const shiftPosition = require('./shiftPosition')
const createShiftsChain = require('./createShiftsChain')
const throwError = require('./throwError')
const parseOption = require('./parseOption')

const utilsIndex = {
    shiftPosition,
    createShiftsChain,
    parseOption,
    throwError,
}

module.exports = utilsIndex
